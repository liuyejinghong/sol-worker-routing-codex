# 私有量化 Agent 编排评测：研究与实施设计草案

> 本地存档说明：2026-09-11 从用户指定的 ChatGPT 会话附件预览提取完整正文，恢复为 Markdown 排版；并非下载所得原始文件的字节副本。来源：https://chatgpt.com/c/6aa370e0-48d4-83ec-92a4-31087f090434 。以下为 GPT 网页版草案，所述仓库与外部来源结论未在本次存档中独立复核。

日期：2026-09-11  
状态：设计提案；尚未实现或执行该基准，不包含任何模型胜负结果。  
目标工作流：Codex Worker Routing / sol-worker-routing-codex  
已检查源码基线：d058287492a01419922b17100f041abf1ac2d1eb  
范围边界：本次检查了工作流源码、配置及历史 benchmark；没有检查用户量化系统源码、真实策略或 Luna 失败轨迹。仓库版本不代表用户本机已安装版本。

## 1. 要回答的决策

评测单位不是一次模型回答，也不是一个 Worker 的最终输出，而是：

> 同一个原始需求、同一个源代码起点，经过真实工作流，直到形成独立验收通过的交付，整条链路付出了多少资源。

必须同时回答：

- 模型是否正确理解策略与交易系统语义？
- 正确任务包交给某个 Worker 后，是否能可靠实现？
- 引入任务拆分、交接、Review、返工和主控接管后，整套组合是否更划算？
- Token 增加时，是否确实换来了质量或时间收益？
- 哪些任务应该保留在主控，哪些值得交给哪种 Worker？

“某类任务不值得委派”是有效评测结果，不是项目失败。不得预设多代理必然省钱，也不得以公开榜单代替这些问题的答案。

## 2. 已检查仓库的事实与缺口

### 2.1 可以复用的基础

- 路由 Skill 已区分主控的需求、业务语义、根因、架构和最终验收责任，与 Worker 的有界实现责任。[R2]
- 已规定：当验收需要重做核心推理时，应由主控直接处理；反复重大返工不应靠无限加长任务包解决。[R2]
- 原生 Luna Max 配置为 `gpt-5.6-luna`、`max`；它承接已确定行为与设计的开发包。[R3]
- 外部 OpenCode Worker 0.3.1 具有任务、请求、轮次、会话和父子会话身份，保留 diff 与结果；`completed` 与验收通过分离。[R4–R6]
- `model_mode=single` 禁止 OMO 内部委派，使用选定 profile 的 Sisyphus 模型；适合做单层 Worker 对照。不同模型应使用隔离评测 profile，不修改用户日常全局 profile。[R4]
- OMO 模式包含外部主控和直接子代理，不等于“一个第三方模型调用”。[R1、R4]

### 2.2 现有 benchmark 不能证明的部分

2026-08-09 报告只有两种机械任务的成对测试，每种每个 Worker 各一次；报告明确未计主控拆分、整合和完整账单。它支持那些狭窄任务的局部观察，不能证明量化系统端到端委派收益。[R7]

### 2.3 具体计量缺口

- `src/core.ts` 的公开回执标记 `cost: 'unavailable'`、`acceptance: 'pending'`。[R5]
- `src/runner.ts` 在子会话行保存原始 `tokens` 数组，但根会话行未保存同等字段。[R6]
- 当前外部插件回执不构成 Codex 主控、原生 Luna、外部 OMO 根和所有子会话的统一账本。
- 轮询得到的状态快照不是新的推理调用；不能反复相加。
- 回执中的文本和工具输出有截断，不能把用于展示的回执当成完整评测证据。[R5]

优先补充旁路计量与检查点，而不是把“记录 token”的工作交给主模型或新增一个记账 Agent。

## 3. 相关研究与复用边界

### Market-Bench

研究已经包含自然语言策略与市场假设到回测代码的任务，并区分“程序可执行”与“损益、回撤、持仓路径符合参考结果”。其任务是入门型策略，不等于用户的复杂交易系统与主从返工链。[W1]

### BacktestBench

研究覆盖自动化策略回测、金融指标和策略参数等任务，可以借鉴题目组织方式；它不直接提供用户私有代码库上的编排经济性结论。[W2]

### SWE-rebench

以持续收集较新的软件工程任务降低污染风险。可借鉴时间切分和题库更新，不应把“题目新”视为绝对无泄漏证明。[W3]

### 评测基础设施

Harbor 支持任务环境、自定义 Agent 接入以及独立 verifier 环境，可作为外层运行器。应接入真实 Codex + 当前插件，而不是为了接入 benchmark 重写生产编排。[W4、W5]

Anthropic 的 Agent eval 方法强调模型与 harness 的组合、重复试验、确定性与人工校准评分。可作为方法参照，具体任务、成本和路由规则仍须由本项目定义。[W6]

## 4. 三条互补评测轨道

### U：策略理解与澄清

输入为用户自然语言描述、允许读取的仓库与文档。不能把人工标准策略契约作为已知答案交给模型。

观察：

- 生成的简明行为约定是否忠于原始描述；
- 对指定行情/订单事件应采取什么行动；
- 哪些决定已由代码或文档确定，哪些需要真正澄清；
- 是否凭空改变执行时点、方向、仓位计算或风控语义。

遇到刻意保留的业务歧义，用预先写好的“用户事实卡”回答澄清，不让自由发挥的模拟用户向某个方案额外透露解法。每个方案拥有同样的事实接口、问题限制和信息边界。无法从允许信息确定的事项，不得在隐藏测试中暗自指定唯一答案。

本轨是定位工具；不能只凭模型自述正确就认定代码正确。

### W：正确任务包下的 Worker 能力

提供人工确认的目标、接口、关键行为、代码起点、范围及验收条件；不给参考补丁或隐藏测试实现。比较主模型与各 Worker 在相同条件下实现的质量和资源。

这是对“理解任务已经正确之后，Worker 会不会做”的条件评测。它不替代端到端成本，因为真实主控生成任务包要付出成本。

对争议失败，可额外做配对回放：同一 Worker 分别接受原始主控任务包与人工修正任务包。若只修正交接信息便显著改善，说明问题可能出在任务理解/传递，而不宜一概归因 Worker。这样的失败诊断子集不能用于宣称整体模型排名。

### E：端到端工作流

从原始需求起步，包含：理解、澄清、探索、拆分、派发、实现、自测、Review、纠正、主控接管、集成和最终交付。

主控允许按冻结的策略选择不委派；记录实际委派率和路由理由。“强迫每道题都派给 Worker”的诊断实验必须另列，不能冒充正常工作流结果。

每次运行结束后，在不泄漏测试答案的外部环境独立验收。

## 5. 对照组设计

令 M 表示用户选择并固定版本、推理档的主模型。不要因为仓库旧名含 Sol 就强制指定 Sol。

| 组 | 执行方式 | 主要回答的问题 |
| --- | --- | --- |
| A | M 单代理，保留正常自测与自查 | 当前任务由主模型独做的基线 |
| B | M 主控 + 同一个 M Worker | 上下文拆分、交接和调度本身是否值得 |
| C | M 主控 + 原生 Luna Max | 用户当前主要疑虑 |
| D | M 主控 + Muse 单层外部 Worker | 更换 Worker 后的整条链路效果 |
| E | M 主控 + DeepSeek 单层外部 Worker | 同上 |
| F | M 主控 + GLM 单层外部 Worker | 实际路由验证通过后再加入 |
| G | M → 外部 OMO 主控 → 内部 Worker | 当前嵌套编排相对单层的净价值 |

首轮只跑 A/B/C 和一个最关心的第三方候选，避免一开始做全模型笛卡尔积。B 组必须确认实际原生路由能够使用 M，否则标为暂不可用，不能悄悄换模型。

模型比较尽量使用同一工具环境。若一个方案是原生 Codex，另一个通过 OpenCode/OMO，结果首先解释为系统组合差异，不能把差异全部归因模型智力。

并发数、写入所有权和资源上限固定。现有外部链路同一时间只有一个写执行者，不能假定增加子代理便产生并行写代码收益。[R4]

## 6. 题库：代表性集合与失败诊断集合分开

### 6.1 任务来源

- 真实历史开发任务，回到修复前的代码与当时需求。
- Luna 已被打回的案例，作为失败诊断开发集。
- 真实顺利交付及无需委派的普通任务，避免只收集困难失败。
- 新设计的非敏感策略变体，增加新鲜性并测试关键语义变化。

任务应从用户工作分布抽样；不要用诊断困难集的失败率估计日常开发成本。

### 6.2 首个扩展题库的示意组成

以下是建议分配，不是统计充分性承诺：

- 原始策略描述到实现：8 个独立任务家族。
- 订单生命周期、风险约束和幂等修复：8 个。
- 回测与运行路径、跨模块行为一致性：6 个。
- 受约束性能改进，行为必须保持：4 个。
- 代码审查：4 个，同时包含已知无实质问题和存在确定问题的补丁。

长上下文、多轮澄清、跨语言依赖、错误恢复作为交叉标签，不是塞无关文字凑长度的独立题型。保留真实相关代码和历史；记录实际峰值上下文、累计输入、压缩次数与重读。不能以短函数题替代用户的大仓库场景。

### 6.3 数据切分

- 开发集用于改提示词、路由和题目评分器。
- 留出集用于冻结配置后的最终对照，不能反复看失败再调参。
- 同一策略/Issue 派生变体必须在同一分区，不能一半训练路由、一半宣称全新测试。
- 定期加入新任务；已被反复优化的留出题应降格为回归题。
- 保存允许的历史，但移除解决当前题目的未来提交、远程答案、其他试验结果和参考补丁。

## 7. 量化行为的可执行真值

### 7.1 示例：不是用户真实策略

假设某道题明确约定：

- 使用已收盘的一小时 K 线。
- 当前收盘价严格大于此前 20 根 K 线的最高价，才出现做多信号；此前窗口不含当前 K 线。
- 于下一根 K 线开盘按给定成交规则执行。
- 同一个收盘事件重复送达，不重复下单。
- 若在第 k 根完成平仓，第 k+1 与 k+2 根禁止新开仓，第 k+3 根可以。
- 其他仓位、成交、费用和冲突优先级由题目明确规定。

模型可以生成完全可运行的代码，却在任意一个时间、窗口或事件边界上实现错。

### 7.2 主要验收对象

不要只看最后总收益；对比关键事件轨迹：

`event → signal → order intent → fill → position → cash/fee/risk state`

用人工可检查的短行情和订单事件序列，覆盖相等边界、未收盘波动、重复事件、下一时点成交与冷却边界。对真实项目的更复杂题，再加入契约要求的部分成交、撤单竞争、重启恢复与多币共享约束。

只测试契约要求和系统可达行为；不要求每个小任务都跑所有风险场景。

### 7.3 真值建立与校准

- 用户或领域审查者确认简洁的行为表与关键预期轨迹。
- 用小型、可审查的参考实现产生补充测试，避免直接照抄候选模型实现。
- 正向对照：已知正确实现必须通过。
- 负向对照：改变 `>` 为 `>=`、取消去重、错移执行一根 K 线等明确错误必须被相关测试抓住。
- 数值容差、交易精度、排序和虚拟时钟在题目中固定。
- 通过合法的未来数据扰动检查“不改变过去决策”等性质；仅在策略契约本身适用时采用。
- 既测试“不该下单时不下单”，也测试“必须下单时确实下单”，防止拒绝一切或永远空仓获得假安全分。

不能让同一个模型同时不经核验地生成需求解释、参考答案与裁判，形成自证循环。隐藏的是测试样本和实现，而不是业务要求。

### 7.4 评分方式

关键业务约束是独立门槛。未来信息泄漏、错用仓位所有权等关键失败，不应被格式分、文档分或大量无关通过测试抵消。

在通过关键门槛之后，可以报告行为覆盖、回归、必要测试和维护性等分项。维护性应有具体标准，不能仅由 LLM 觉得“更优雅”决定。

忠实实现一个亏损策略可以通过；因为偷看未来而盈利更高的实现必须失败。此评测衡量实现正确性，不衡量投资建议或策略盈利能力。

## 8. 返工、Review 和责任定位

### 8.1 保存检查点

为每个开发子任务保留：

- C0：进入任务时的基线与约定；
- C1：Worker 首次声称完成、主控尚未修改时的补丁；
- C2…Cn：每次明确纠正后的补丁；
- CF：整条工作流的最终交付。

对这些不可变副本离线评分，评分过程不向正在运行的方案反馈隐藏答案。主控接管必须记录边界，不能把接管后的成功归为 Worker 一次通过。

### 8.2 独立裁判与过程 Review 分离

过程 Reviewer 是被测工作流的一部分，其调用、误判与返工成本计入工作流。最终评测裁判在工作流之外，费用作为评测开销另列，不与被测运行成本混算。

| 主控判定 | 独立证据 | 解释 |
| --- | --- | --- |
| 打回 | 相同约定下确实不合格 | 有效纠错，继续定位根因 |
| 打回 | 相同约定下满足所有已定义要求 | 疑似误打回，需要核查 Review 理由 |
| 接受 | 确实不合格 | 漏检，不能因为没有返工就给高效率评价 |
| 接受 | 合格 | 正常交付 |

隐藏测试通过不自动否定 Reviewer 发现的新真实问题；若裁判遗漏必要约束，先修正题目和对照，再决定是否误打回。不得用一套不完整测试替主控定罪。

### 8.3 根因标签

标签以证据为准，允许多个原因或 unknown：

`strategy_misunderstanding`、`packet_omission`、`worker_implementation_error`、`integration_error`、`review_false_positive`、`review_escape`、`harness_or_permission_failure`、`infrastructure_failure`、`unknown`。

不能仅因“主代理说它错了”就贴 Worker 错误标签，也不能仅靠失败模型自己的自我分析归因。

## 9. 计量合同

### 9.1 完整范围

计入 Codex 主控、所有原生子代理、外部根会话、所有 OMO 子会话、Follow-up、模型自我修复、主控接管、摘要/压缩等辅助模型调用。被取消或报错但已产生用量的调用也计入。

程序内部空轮询主要产生系统开销，不自动产生模型 token；因轮询结果返回给模型而触发新的推理调用，才有相应模型消耗。

### 9.2 推荐事件记录

以下是待实现的数据契约，不是现有插件已经支持的参数：

```json
{
  "schema_version": 1,
  "run_id": "opaque-id",
  "task_id": "private-task-id",
  "arm_id": "M_plus_worker",
  "trial_index": 0,
  "session_id": "opaque-session",
  "parent_session_id": null,
  "call_id": "unique-model-call",
  "attempt_index": 0,
  "role": "coordinator",
  "phase": "initial_implementation",
  "provider": "observed-provider",
  "requested_model": "configured-model",
  "observed_model": "returned-model",
  "reasoning_effort": "observed-or-configured",
  "started_at": "timestamp",
  "ended_at": "timestamp",
  "raw_usage": {},
  "normalized_usage": null,
  "usage_status": "missing",
  "billing_source": "unavailable"
}
```

不要要求模型填写 token 数。优先客户端或服务端原始 usage；只有估算值时必须标注估算方法，不能伪装实际账单。

### 9.3 去重和字段语义

- 相同调用的重复快照只计一次，真实重试则按 attempt 独立计入。
- 先确认主线程总数是否包含子代理；包含时不能再次叠加子代理。
- 输入的缓存命中通常是总输入的子集，不能无条件相加。
- OpenAI Responses API 的 reasoning token 已包含在 output token 总数中，不能再加一次；其他客户端和历史字段必须逐一核验。[W8]
- 不从文件字节大小推定准确 token；不同模型 tokenizer 不同。
- 缺失即 null/unknown，不填 0。报告缺失调用数、哪些分支未覆盖；缺关键分支时不发布准确的全链路节省百分比。

### 9.4 数据获取

Codex 官方非交互 JSONL 输出包含 turn 完成和 usage 等事件，可作为主控采集起点，但是否覆盖所有原生子代理须以固定客户端版本校准。[W7]

外部插件需补齐根与子会话统一用量结构、稳定调用身份和失败结束时的用量收集。先做单调用、父子调用、Follow-up、失败、取消、重复轮询的计量夹具测试，再进行付费真实题目运行。

不为获取漂亮日志悄悄更换认证方式、订阅 Provider、模型、推理档或生产 harness。任何必须改变执行路径的评测适配都作为实验条件披露。

## 10. 指标与成本模型

### 10.1 主报告

每组报告：独立最终通过率、关键业务缺陷、Worker 首次交付通过率、主控接管率、全部运行资源、成功交付资源、端到端时间与人为干预。

设一次完整运行 j 的全部模型费用为 C_j，独立通过标记为 A_j∈{0,1}：

`本批次每个合格交付的成本 = Σ C_j / Σ A_j`

费用分子包含最终失败的任务、返工、超时、取消和接管。全部失败时，指标不可用/无穷，不记为零。

Token 可以同样统计，但需保留模型与计费类别分解。该比值是这批任务的资源产出效率，不是“同一道题可以独立无限重试”的精确期望成本。

### 10.2 三本账

- 原始模型用量：各模型的输入、缓存和输出，不能替代实际账单。
- 实际费用/订阅额度：实际新增付款、可观测的 quota 消耗和限流情况分别列出。
- 标准费率等价成本：只作估算，用冻结的当日价格快照，不称为订阅真实支出。

不同订阅额度不能在没有汇率定义时简单相加；月费分摊方法也必须单列。边际付款为零不代表速度、稀缺额度或机会成本为零。

### 10.3 时间与质量

- 墙钟从原始任务开始到该运行最终交付，包含编排等待和必要 Review；不能把并发子任务耗时直接相加当成完成时间。
- 隐藏评分器运行时间另列，避免用离线评测开销扭曲生产速度。
- 失败不能从时间统计中消失。主图采用“给定预算/截止时间内有多少任务通过”，成功任务中位时间等仅作带条件的补充。
- 程序测试与编译成本、CPU 争用及人类纠正分钟数单列。
- 多轮尝试只挑最好一次会夸大常规可靠性；评测以完整运行的实际交付结果为单位。

### 10.4 不预设综合分

先看关键质量门槛，再比较成本、时间和正常使用成功率。质量提高但费用上升属于取舍，不伪装为省 token。对成本更高但可靠性更高的组合，保留为另一条可选边界。

## 11. 公平性与统计

每个对照固定同一个任务家族、精确代码起点、允许信息和验收要求；同一组固定模型、推理档、Skill 与插件版本。记录实际安装缓存版本，不能仅记录开发仓库 SHA。

每次运行使用独立工作副本、会话、工作状态和允许的 HOME；认证只在最小范围注入，不能把真实交易所密钥或用户完整 HOME 挂入环境。不得连接真实资金或生产交易服务。

端到端资源预算覆盖整棵调用树，不能给每个 Worker 一份额外完整预算然后称它胜出。若比较不同预算，应画预算—成功率曲线。

交错/随机化同题各组运行顺序；固定本机计算资源、并发上限，记录限流、排队与缓存。无法证明清除了服务端缓存时，不声称绝对冷缓存；也不能把串行第一组与普遍命中缓存的最后一组直接比较。

统计以任务/任务家族为配对与重采样单位，重复试验不能伪装新增独立题目。报告配对差值、不确定区间和样本数。小样本中差异不清楚，应结论为证据不足，而不是强行排名。

如果用不劣性门槛，应在看最终数据前设定可容忍的质量下降范围；资金安全相关核心语义不能靠更低均值费用补偿。仅比较“所有方案都通过的子集”可作为附表，不能代替全体结果。

基础设施失败需单独分类。真实使用会遇到的故障属于系统可靠性成本；纯评测器故障的重跑规则预先约定且对所有组一致，不能选择性丢弃结果。

## 12. 保密与污染边界

仓库与官方 OpenCode Go 文档都说明 Muse Spark Contributor 允许输入输出用于未来模型训练。[R1、R4、W9]

因此：

- 长期保密策略和私有留出题，只进入经用户批准且满足数据条件的路径。
- Contributor 对照使用非敏感、新建、可消耗的任务；仅更改币种或变量名并不自动消除策略机密。
- 同一对照必须用同一可授权任务集，不能让 Muse 做合成题、其他模型做保密难题后直接比较分数。
- 不共享隐藏答案和独立评分器；对运行轨迹、补丁、题目和报告同样做数据分级。
- 允许未来训练不等于证明当前模型已经见过某一道题，但会影响长期保密与未来复测的留出属性。

## 13. 最小落地架构

```text
私有任务包 / 原始需求 / 固定环境
                 │
          外层评测运行器
                 │
   真实 Codex + 冻结 routing Skill
           ├─ 原生 Worker
           └─ 外部插件 → OpenCode / OMO
                 │
      旁路用量账本 + 交付检查点
                 │
       隔离的确定性行为裁判
                 │
   分任务类别的质量 / 成本 / 时间报告
```

建议选择一个外层运行器，优先验证 Harbor 自定义 Agent 接入是否能保留实际认证与插件行为；若最初适配代价过高，可先用薄的本地进程运行器验证同一任务合同，后续再迁移，不同时维护多个重型评测平台。

核心只负责运行、计量、检查点与汇总，量化规则放在私有领域包。不要硬编码某个主模型或把参考策略塞进生产路由 Skill。

参考目录是待开发结构，而非已存在文件：

```text
quant-agent-eval/
  taskpacks/quant-private/    # 独立任务家族、原始说明、环境定义
  adapters/                  # 真实 CLI/工作流薄适配
  telemetry/                 # 原始事件、归一化和去重
  grading/                   # 仅运行器可访问；隐藏行为测试
  experiments/               # 冻结对照组、预算、版本、数据切分
  reports/                   # 本地派生结果；不默认公开
```

评分器应显式放入独立环境；Harbor 默认同容器 verifier，不能只创建 tests 文件夹就假设已隔离。[W5]

## 14. 实施阶段与验收

### P0：证明账本可靠

交付：字段语义文档、调用树采集、去重测试、缺失用量报告。

验收：单代理、原生子代理、外部单层/嵌套、Follow-up 和失败结束的用量均可追踪；不能覆盖之处明确声明。不得以模型自报补齐。

### P1：证明裁判可靠

从 8–12 个真实/非敏感案例开始，兼有失败、成功与小任务。逐个确认原始需求、正确行为表、已知正确补丁及错误变体。

验收：已知正确通过，关键已知错误被拒；不存在隐藏但未告知的业务要求。完成交付前后检查点离线评分。

### P2：小规模对照，定位大问题

只跑 A/B/C 和一个第三方候选，适量重复以观察波动。先比较固定正确任务包，再比较端到端链路。

验收：能区分 Worker 真错、主控理解/交接错误、Review 误判、接管救回和 harness 故障。小试点不发布稳定排行榜或普遍节省百分比。

### P3：冻结配置，扩大留出对照

按预算扩到约 30–50 个独立任务家族及最有希望的少数组合，多次运行。该范围只是规划量级；所需样本取决于实际差异和波动，不能保证足以判断几个百分点的改善。

验收：输出配对结果与不确定性，保留失败成本，报告分任务类别的委派建议。主模型、Worker、路由或插件变化后，用冻结回归集检查变化；不能把反复调过的回归集继续当全新能力测试。

## 15. 最终报告必须能形成的结论

以下都是判读规则，不是已测结果：

- 正确任务包下 Worker 仍差：该任务类型优先保留主控或换 Worker。
- 正确任务包表现好，端到端表现差：检查主控理解、任务切分、信息丢失及集成。
- 首次补丁合格但频繁被打回：校准 Reviewer，而不先增加 Worker 推理强度。
- 同模型拆成主从仍无质量/速度收益且更贵：当前任务粒度的编排开销不值得。
- Token 增加，但相同质量下明显更快：明确报告速度收益，而非 token 节省。
- 费用上升但关键失败减少：报告更高可靠性方案，不与低价方案混成单一排名。
- 差异小于当前不确定性：暂时保留原路由，结论写证据不足。

最终产物应是“任务类别 → 候选执行方式 → 有证据的取舍”，而不是一个总分模型榜。

## 来源索引

所有仓库路径均按本设计顶部的固定提交检查。网址以代码形式保存，便于手动核验。外部文档的行为和政策未来可能变化，实施时应保存快照。

### 仓库来源

仓库根：`https://github.com/liuyejinghong/sol-worker-routing-codex/tree/d058287492a01419922b17100f041abf1ac2d1eb`

- [R1] `README.md`
- [R2] `skills/sol-worker-routing/SKILL.md`
- [R3] `agents/luna-worker.toml`
- [R4] `plugins/opencode-worker/README.md`
- [R5] `plugins/opencode-worker/src/core.ts`
- [R6] `plugins/opencode-worker/src/runner.ts`，尤其根/子 SessionResult 构建与结果持久化。
- [R7] `benchmarks/report-2026-08-09.md` 与 `benchmarks/README.md`

### 公开一手来源

- [W1] Market-Bench：`https://arxiv.org/abs/2512.12264`
- [W2] BacktestBench：`https://arxiv.org/abs/2605.17937`
- [W3] SWE-rebench：`https://arxiv.org/abs/2505.20411`
- [W4] Harbor Agent 接入：`https://www.harborframework.com/docs/agents`
- [W5] Harbor 任务与独立 verifier：`https://www.harborframework.com/docs/tasks`
- [W6] Anthropic Agent eval 方法：`https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents`
- [W7] Codex 非交互执行：`https://developers.openai.com/codex/noninteractive`
- [W8] OpenAI reasoning usage：`https://developers.openai.com/api/docs/guides/reasoning`
- [W9] OpenCode Go 模型与数据政策：`https://opencode.ai/docs/go/`

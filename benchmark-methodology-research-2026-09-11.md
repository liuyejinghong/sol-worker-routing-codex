# 软件工程与量化 Agent Benchmark：开放性、流程与设计启示

核查日期：2026-09-11。依据官方项目、原始论文、维护者文档与公开审计；本轮未安装框架、运行模型或复现排行榜。服务当前文档：[设计草案 v0.4](quant-agent-routing-benchmark-design-v0.4.md)。本文区分来源事实与本项目建议，不将研究建议自动视为用户已确认的规则。

## 1. 结论：没有一套能包办我们的目标

“最权威”需要区分行业认知、可复现性、题目质量与领域适配。对当前任务，建议研究三个通用方向：SWE-bench 系列的真实仓库修复与独立测试，Terminal-Bench/Harbor 的标准任务与自动执行，FrontierCode 的交付质量与裁判校准。量化题源则继续筛 BacktestBench 与 Market-Bench。

这不是为这些项目编制另一张权威排行榜。尤其不能将厂商自称的领先或最可靠当作独立共识。FrontierCode 由 Cognition 维护且题库不公开，本地可复核程度与开放题库不同。

我对我们方案的建议是：借鉴开放框架的运行和产物合同，借鉴 FrontierCode 的质量评分方法，以经核验的量化题和真实事故建立自己的标准答案。分级返工扣分仍是我们的定制设计，需要自行验证。

## 2. 开放到什么程度

| 项目 | 已核查的开放范围 | 对我们的价值与边界 |
| --- | --- | --- |
| SWE-bench / Verified | 官方提供数据、评测文档与榜单；Verified 为人工筛选的 500 题 | 成熟的仓库问题评测参照；不能仅凭名气认为答案无误。[官方入口](https://www.swebench.com/) |
| SWE-bench Pro | 公共集、gold patches、运行脚本、Docker 镜像与评测代码；另有商业私有榜 | 公共部分可复现；不是全量私有题也开放。代码仓库标 MIT，第三方仓库和数据仍有自身边界。[官方仓库](https://github.com/scaleapi/SWE-bench_Pro-os) |
| Terminal-Bench | 题库仓库含 tasks、贡献规则、Apache-2.0 许可；使用 Harbor 运行并发布带版本的数据集 | 很适合借鉴任务组织。当前主项目采用持续发布，不应将 2.0 或 2.1 说成永远最新，也不应对正式成绩使用浮动 latest。[官方仓库](https://github.com/harbor-framework/terminal-bench) |
| FrontierCode 1.1 | 方法、榜单与示例公开；完整任务明确不计划公开，以降低污染 | 最贴近“正确之外还要可维护”的要求，但不能直接下载完整套件在本地跑。[方法说明](https://cognition.com/blog/frontier-code) |
| BacktestBench | 公开仓库含 AutoBacktest 与数据文件 | 可作为量化题源；大文件、原始数据库、答案复现与复用条件未在本轮完整核验。[数据目录](https://github.com/jensenw1/BacktestBench/tree/main/datasets) |
| Market-Bench | 论文、三类策略和排行榜公开 | 适合参考交易行为验收；尚未确认完整可下载的代码、数据与答案发行包。[官网](https://www.marketbench.ai/) |

“论文开放”“任务文件可见”“运行器开源”“标准答案可复现”必须分列。只有公开榜单不能算开放 benchmark；公开 gold patch 也不意味着可以把它装进参赛环境。

## 3. 各自怎样运行

### SWE-bench Pro：解题与验收分开

其公开流程是：准备问题与对应仓库环境 → 用自选 harness 生成补丁 → 汇集补丁记录 → 在 Docker/Modal 或本地 Docker 中执行评测 → 保存结果。官方同时提供 gold patch 校验路径。生成答案的系统与评测补丁的脚本分离，便于更换 Agent 而保持验收一致。[运行说明](https://github.com/scaleapi/SWE-bench_Pro-os)

对我们的建议：四个 harness 不必接受同一条底层命令，只要能产生统一身份、题目提交和证据，之后交给同一评分系统。不要让主持 Codex 一边读答案，一边给参赛者改题面或补充建议。

### Terminal-Bench/Harbor：任务是完整可执行包

Harbor 将题面、配置、环境、参考解和测试分别组织；测试可输出单值或多个数值指标。独立 verifier 环境是显式配置，默认并不与参赛环境分离。[任务结构](https://www.harborframework.com/docs/tasks)

一次运行选择数据集、Agent 与模型，产生 job/trial 配置、结果、轨迹和 verifier 输出；官方 viewer 可查看用量、耗时、错误及产物。[运行与结果](https://www.harborframework.com/docs/run-jobs/run-evals)

当前文档已列出 Codex CLI 和 Grok Build，支持扩展外部 Agent 或容器内安装的 Agent；这只是现成适配基础，不证明用户本机认证、Skills/MCP、原生子代理已经无损兼容。[Agent 接口](https://www.harborframework.com/docs/agents)

其 ATIF 轨迹格式涵盖消息、工具调用、观察及模型用量等信息。[轨迹格式](https://www.harborframework.com/docs/agents/trajectory-format)

对我们的建议：先评估复用这些合同和已有组件，不急着同时自建运行器、轨迹规范和报告 UI。是否直接采用 Harbor，要用用户实际环境验证；若无法保留指定配置，就借鉴结构而保留薄适配。数据库存题目级评分和版本关系，原始产物继续留存，不必将已有轨迹转写为另一个丢失细节的格式。

### FrontierCode：正确性门槛之上的工程质量

FrontierCode 由仓库维护者制定任务和评分标准，使用测试及 rubric 评价。原始方法先判断 blocker，通过后再算加权分。裁判质量流程包括故意错误解、另一种合法正确解，以及不同质量的校准解。完整任务不公开。[原始方法](https://cognition.com/blog/frontier-code)

1.1 保留正常联网，区分查资料和查同题答案，并检查后者；同时修订了过严的 blocker，说明工程质量裁判也必须迭代。[1.1 修订](https://cognition.com/blog/frontier-code-1.1)

对我们的建议：功能正确与工程质量分开；最终错按用户规定的独立题目零分，功能正确但过度复杂化再扣质量分。不要把每个维护性偏好设为“一票否决”，否则又变成错误地打回正确实现。联网与 Skills/MCP 照常允许，答案暴露另行判定。

### 量化题：比较行为，不把收益当正确性的替身

Market-Bench 从策略描述生成回测器，对照参考输出，涉及订单簿流动性、交易延迟及持仓/现金等状态。BacktestBench 则组织指标计算、选股、策略选择和参数确认任务。[Market-Bench](https://www.marketbench.ai/)、[BacktestBench 论文](https://arxiv.org/html/2605.17937v1)

对我们的建议：优先保留可手工核验的状态轨迹，再辅以收益、现金和费用等聚合结果。两个实现收益相同，仍可能在执行时点或重复下单上不同。题目可以很短，但验收不能只检查最终一个数字。

## 4. 权威项目也会修错题

OpenAI 2026-07-08 的审计估计 SWE-bench Pro 公共集约三成任务有问题，并撤回此前推荐。问题包括题面与测试不一致、隐藏要求、测试过严或覆盖不足。这个估计属于 OpenAI 的审计结论，不是所有参与方已经一致认可的定论。[审计原文](https://openai.com/index/separating-signal-from-noise-coding-evaluations/)

Terminal-Bench 2.1 官方修订了 2.0 的 89 题中的 28 题，涉及外部依赖漂移、资源不匹配和题目说明问题。[版本修订](https://www.tbench.ai/news/terminal-bench-2-1)

9 月 8 日还有新论文 SWE-Bench Pro Verified，尝试同时修复题目质量和泄漏通道。它刚发表，不能因名称带 Verified 就立即认作新的权威定论；本轮只确认论文提出的方法，未复现其结果。[论文](https://arxiv.org/abs/2609.08149)

对我们的直接约束是：真实事故与历史 PR 都不是天然标准题。事故报告可能混有中途误判，历史测试可能只认可原作者实现。题目上榜前必须重新建立“题面—允许上下文—必要行为—评分器”之间的一致关系。

## 5. 建议采用的出题流程

以下是依据上述经验提出的本地设计，不是现成 benchmark 已替我们完成的功能。

1. **选择一个明确目标。** 从公开题或真实事故截取可独立交付的问题，明确必要输入和产物。一个长事故可提炼几道独立题，不强迫每题重建整个交易平台。
2. **先确定行为标准。** 每个得分点能追溯到题面或允许资料。裁判可以藏样例，但不能藏业务要求。
3. **建立已知正确解。** 用可检查的实现和短事件轨迹验证答案，不把现有代码自动当真值。
4. **建立反例和替代解。** 错误实现必须失败；不同结构的正确实现必须通过。补一个“功能正确但明显过度复杂”的实现，确认工程质量分能识别差异。
5. **校准等级。** 给题目难度、错误等级和维护性扣分写证据依据。多个校准实现的相对排序不合理，就先修规则。
6. **冻结版本。** 固定题面、数据、环境、分值和裁判。发现题目缺陷发布修订，不静默改写旧分。
7. **正式运行一次。** 按用户指定配置正常解题，采集过程，封存产物，之后独立评分、入库和出报告。

出题阶段运行参考解和错误夹具，不等于要求用户模型反复考试。继续保持“一次只跑一个指定配置”，不引入强制组合、自动多轮择优或完成时间硬上限。

## 6. 对当前草案的指导性修正

### 先校准裁判，再优化权重

若裁判把错误实现判正确，任何最优权重都无意义。我们先制作以下校准集合：简单且正确、另一种正确结构、正确但无必要地复杂、基础错误后修好、复杂错误后修好、最终仍错。通过这些样例验证等级和相对顺序，才确定奖励、罚分与效率换算。

参考解应覆盖“必须做时确实做”与“不该做时没有做”。这可防止永远不下单、只写空实现、返回固定答案等获得虚假的安全分。

### 返工扣分是定制项，不是拿来即用的行业标准

本轮检查的主要流程集中于最终交付评价，没有发现可以直接采用的“低级错误重扣、复杂错误标准扣”统一规范。用户的偏好可以实现，但必须建立可观察证据、错误去重与裁判校准。

尤其应避免把合法试验、测试驱动开发的预期失败、尚未形成候选产物的草稿误当返工。否则勤于验证的 Agent 会比不测试的 Agent 更吃亏。观察不足不能等于零返工；不同 harness 的日志详略也不能变成能力差异。

### 维护性评分应针对当前任务的必要性

“未来 bug 率更高”通常不是一次小测试能量化证实的结果。我们可以量化已定义的复杂度负担，例如重复状态、无任务依据的扩展层或不必要依赖，但必须说明这是维护性代理指标，而非测得未来事故率。

每项质量扣分应包含位置、规则和简化方向；允许多种正确设计，必要的可靠性处理不罚。不要由评分器任意改候选代码后再宣布它通过：适配只处理约定接口；若题目无法容纳合法实现，修题并保留版本。

### 开放工具与防止查答案同时保留

允许正常搜索不代表能保证公开题不被检索命中。正式题应登记已知原始 PR、参考答案或含答案的新版本来源，保存可见的搜索和下载证据。遇到疑似命中需要判定具体证据，不能把整个 GitHub 或包管理器都视为违规。

开放题适合调试与方法比较；要测陌生问题能力，应有新提炼并核验的题目。所有配置仍然使用同版同题，不因模型名字不同而换题。

### 小题库允许单次排名，但结论应匹配范围

单次成绩可排序，无需为了像公开榜单而强制重复。但我们只能称它为该版本、该配置的一次结果，不能由五六个包推导整个量化开发领域的稳定胜率。难度奖励和分项结果保留，避免一个总分遮住基础题与复杂题的差异。

## 7. 推荐落点

目前最值得优先借鉴的是 **Harbor 的任务与产物合同、FrontierCode 的裁判校准、SWE-bench 的补丁独立验收，以及量化 benchmark 的行为真值**。

在建设跑分平台前，先完成少量候选题卡与裁判样例，验证我们的规则能把“错误、修正后的正确、简单正确、过度复杂但正确”区分开。四个 harness 的自动化能力盘点可以随后或同时进行，但不需要先做完整矩阵、公开网站或复杂基础设施。

此建议补充 v0.4，不自动改变已确认的单次运行、正常工具开放、题目级零分、双榜和版本数据库要求。

# 量化 Agent Benchmark 工作计划 v0.1

日期：2026-09-11。依据 [方案 v0.5](quant-agent-routing-benchmark-design-v0.5.md)、[架构 v0.1](quant-agent-benchmark-architecture-v0.1.md)与[方法研究](benchmark-methodology-research-2026-09-11.md)。本次交付是工作顺序规划，未开始新项目创建、代码实现或真实模型评测。

## 1. 推荐顺序

独立项目整理 → 题源与环境盘点 → 一个代表包和实际采集验证 → 最小端到端闭环 → 完整精简题库与评分定稿 → 冻结首版并执行一次指定配置。

先用一小段完整链路验证题目、过程采集和评分是否可行，再完成全部题包。这样既不会先造大平台，也不会把整套返工评分建立在尚不可观察的事件上。

出题与环境盘点是两项可独立推进的工作；评分协议最终要结合真实采集能力定稿。这里描述工作依赖，不表示已派发任何并行 Agent 或外部任务。

## 2. 分阶段交付与完成标准

### P0：整理独立项目

**目的：** 建立后续实现的唯一工作位置，保持与 codex-workflow、AItrading 的边界。

工作：

- 建立独立本地项目，整理当前方案、架构和研究来源；项目名可暂用 `quant-agent-bench`。
- 保留原始草案和修订来源，但当前入口只指向一份方案、一份架构、一份计划，后续用 Git 历史管理修改，避免每次复制一整套长文档。
- 明确本地题目、答案、运行产物及数据库的位置；敏感产物不默认进入公开仓库。
- 修复迁移后的文档链接，工作流和 AItrading 来源引用保留版本/出处。

交付：项目说明、当前文档入口、目录与数据边界。

完成标准：能明确指出当前方案及后续代码应放在哪里，原始依据可追溯；不创建与首版无关的框架目录和服务。

### P1：题源与现有环境盘点

**目的：** 用可获取的题目和真实环境决定首版范围。

题源工作：核查公开 benchmark 的数据、参考答案、可复现环境和复用条件；从 AItrading 已确认事故中筛选典型。每个候选写出题意、标准答案依据、难度、关键点、公开答案暴露情况，以及能否提炼为小任务。

环境工作：只读盘点 Codex、Grok Build、ZCode、OpenCode 的启动接口、版本和实际配置来源、结果导出、子会话、用量与工作区观察能力。登记现有工作流的源码、安装与加载身份，保留正常 Skills/MCP。

交付：候选题卡清单、四个 harness 能力表、首个验证环境的建议。

完成标准：候选不能只剩论文名称；环境不能只写“支持 CLI”而没有具体入口与采集缺口。已有事实和待验证项明确分开。

### P2：验证一个代表包与评分证据

**目的：** 先证明返工和维护性能判得准、采得到。

工作：

- 选择一个包含几个独立题目的代表包，具有可检查的标准行为，并能自然容纳主控与 Worker 的交接；不强迫委派。
- 制作正确解、合法替代解、必要复杂解、过度复杂解、基础错误修正轨迹、复杂错误修正轨迹及最终错误样例。
- 建立行为评分与分级错误规则样例，校准维护性 Reviewer；权重先保留候选值。
- 用记录夹具校准事件去重、错误与正常实验的区别；随后在用户指定的一个实际配置中验证新会话、工作流身份、提交、过程快照、用量、断线恢复及答案隔离。
- 检查 Harbor/现有适配能否保留用户环境；能复用就复用，存在具体缺口才补适配，不维护两套完整运行框架。

交付：一个可执行代表包、已标注校准样例、观察合同、实际采集验证记录、框架采用决定。

完成标准：最终错题为零，合法替代解不误伤，过度复杂化扣分有具体证据；自纠与主控纠错可按同一口径观察。缺失记录不能被填成零返工或零消耗。

若关键证据采不到，先解决或明确调整可评分范围，不继续生产看似精确的总分。这里的技术验证不是正式排行榜运行，不自动扩成多模型组合。

### P3：实现最小端到端闭环

**目的：** 一次用户请求能形成可靠、可追溯的结果。

工作：只围绕代表包连接运行请求、实际配置快照、执行适配、旁路采集、产物封存、独立评分、SQLite、双榜和报告。评分规则以版本化数据表达，不把尚未确定的权重写死。

用已知产物/事件验证候选失败、用量缺失、重复事件、主持断线和评分失败后的恢复。恢复评分或生成报告使用原产物，不重新解题；不得把旧运行覆盖为更好的结果。

交付：首个可使用的本地运行入口、一份样例详细报告、数据库记录和双榜展示。

完成标准：每个分数可追溯到题目、裁判、工作流和证据；错误题仍保留在成绩中；主持与裁判不向被测主控提供额外答案。执行完成、正确性、证据完整性和排名资格分别体现。

不以接通全部四个 harness 作为这一步的前提，也不做公开网站或远程数据库。

### P4：完成首版精简题库与评分定稿

**目的：** 从技术样例扩展为有代表性的正式 benchmark。

工作：

- 将通过筛选的题目组织成约 5–8 个精简包，每包几个独立题目；覆盖基础和复杂逻辑、实现、事故修复与工程质量。
- 每题按相同流程完成答案、正反例和替代解校准。既保留适合单做的题，也有正常可交接的任务。
- 用虚构成绩验证难度奖励、低级错误重扣、复杂错误标准扣、维护性差异和正确性优先，选定固定权重与效率尺度。
- 固定题目分值和包级资源归属，验证新增参测配置不会反过来改变历史归一化分数。
- 整理题源暴露、裁判争议及版本变更处理规则。

交付：首版候选题库、可执行评分规则、校准结果、示例排名及报告格式。

完成标准：题库每一题都有答案依据；不能靠分值设计掩盖裁判缺陷。全部最终错误得零；已修复错误、难题奖励和工程质量差异均可解释。正常自测和工具使用不因形式本身被处罚。

这里定稿的是评分规则，不使用某模型真实得分来反向调整权重、制造冠军。

### P5：冻结版本，运行一次指定配置

**目的：** 产出第一条正式可追溯成绩。

工作：冻结题库、评分器、运行器和适配器身份；保存实际生效的模型/harness/工作流/工具快照；按用户指定配置顺序执行全部包一次，完成独立评分、入库和报告。

交付：一个正式 run_id、能力榜和综合榜成绩、逐题结果、返工与维护性证据、全链路用量及时间、版本身份和完整报告。

完成标准：用户可以从总分查看到逐题依据。缺失必要证据时，保留可确定结果并明确对应榜单资格，不伪造完整成绩。不会因成绩不理想自动重跑或增加别的配置。

如果实际没有发生委派，明确说明这条记录未验证 Worker 表现，不因此强迫重跑。以后用户指定新模型、harness 或工作流版本，就新增一次运行，与兼容的已有成绩比较。

## 3. 后续扩展顺序

首个正式闭环成立后，再按用户实际要测的环境接入其他 harness，复用同一题库与评分规则。工作流升级属于新的被测配置，实际再跑后比较；换评分权重且证据足够则可以离线重算，保留原分。

公开展示、多人并发、远程调度及更大题库都不是首版前置任务。题库扩充应发布新版本，不暗中改变旧榜单。

## 4. 需要用户参与的少数节点

- 候选题确有业务歧义或答案冲突时，确认真实意图；能够从已确认文档查清的内容由 Agent 自行核实。
- 看具体分级扣分和模拟排名示例，确认是否符合“正确优先、低级错误重扣、难题奖励、简单直接”的偏好。
- 在首次真实调用前指定/确认实际参测配置与调用范围；不要求先选定全部模型与 harness 搭配。

其他常规实现、数据结构、目录和适配选择由 Agent 按已确认目标推进。代码、模型调用、安装、提交推送等执行范围按当时授权处理；本工作计划本身不宣称这些操作已获执行或已完成。

## 5. 当前下一步

开始执行时先做 P0 和 P1，交付独立项目文档入口、候选题卡与实际环境能力表。暂不先写全套评分引擎，也不拿整套模型去试跑未校准的题目。

# 量化 Agent Benchmark 设计草案 v0.3

日期：2026-09-11。状态：需求对齐与设计，尚未实现、选定正式题目或运行测试。

本版在 [v0.2](quant-agent-routing-benchmark-design-v0.2.md) 基础上落实过程返工与最终错误的区别，并评估主持方式、版本数据库和评测隔离，是后续讨论的当前草案。[网页原始草案](quant-agent-routing-benchmark-plan-2026-09-11.md)保留为来源档案。与本版冲突的旧建议不再作为要求，尤其是预设 A/B/C 组合、自动排列模型矩阵、强制重复运行、扩充到 30–50 个任务家族，以及不提供总分排名。

## 1. 产品目标与确认的要求

建立一套可版本迭代、具有标准答案、统一计分的精简量化 benchmark。用户指定一个实际模型与 harness 配置，启动一次，系统按固定顺序执行该版本全部任务包，自动验收、计量、生成报告并登记成绩。以后指定其他配置运行同一版本，即可比较并排序。

| 用户要求 | 本版落实 |
| --- | --- |
| 系统化、版本化、量化复用，同题同标准 | 发布不可变的题库、答案、评分与运行规则版本；各配置使用相同内容 |
| 配好环境后统一启动，自动完成统计 | 一个运行入口，harness 适配器负责执行差异；自动顺序调度、评分和报告 |
| 支持单模型及不同 harness，按实际环境登记 | 成绩绑定实际模型、harness、推理档和模式，不要求覆盖所有搭配 |
| 用户指定什么跑什么，一次即可 | 一次启动仅运行一个配置，每个包一次；不自动发起对照组或重复试验 |
| 若干精简包、每包几个关键点 | 暂按 6 包设计，可在首版定稿时调整到约 5–8 包，不发展成长期大任务 |
| 最终有分数、排序，也保留详细报告 | 每次运行产生固定口径总分、分项得分、资源与时间明细，并进入对应版本成绩表 |

原有三个核心关切继续保留：策略理解是否正确；实现和返工发生了什么；整条执行链的质量、资源与效率如何。返工次数本身不等于错误次数，便宜也不等于正确。

## 2. 一次运行的含义

一次运行 = 一个 benchmark 版本 × 一个指定配置 × 固定任务包序列。

例如用户本次指定 OpenCode + DeepSeek v4.1 Flash + 某推理档 + 单代理，系统只跑这个配置。以后指定 Codex + 某主模型 + 某子代理，就另建一条运行记录。两次只有在题库和运行规则兼容时才直接比较。

“一次”不限制模型只能回答一条消息：包内可有正常工具调用、自测、自查，以及编排模式允许的内部返工；这些都计入同一次运行的资源消耗。禁止评分器公布答案后自动重做一遍取最好成绩。再次运行必须由用户另行发起，并保留原记录。

任务包之间使用独立会话和干净代码起点，按顺序执行，不把上一包答案、评语或探索历史带入下一包。一个包内部可以包含固定的阶段，例如先提交行为判断，再提交实现。

## 3. 被测配置与身份记录

最小配置包括：harness 名称与版本、模型与 provider、推理设置、执行模式，以及需要时的子代理配置。模型名以用户配置和实际回执为准，不由 benchmark 预置候选池。

支持两种执行模式：

- 单代理：一个被测 Agent 完成任务，可正常自测；不委派子代理。
- 编排：主控可按指定规则使用子代理，记录完整调用树及实际参与模型。允许某个包未发生委派。

同模型子代理也属于编排模式。若存在外部主控再委派的结构，同样记录为实际调用树，不额外触发新的测试配置。

每条成绩必须包含配置请求值、可观测实际值、harness/插件/路由规则版本和身份核验状态。推理档不受某 harness 支持时，应在启动前说明并停止该配置，不能静默换档。无法核实实际模型时保留 unknown，不能冒充已确认身份的模型成绩。

单模型能力通过“单代理模式下的该模型与该 harness”测量；同模型换 harness 形成另一条成绩。排行榜展示完整配置，不能把不同工具环境的差异全归因于模型能力。无需为登记一个模型补跑其他 harness。

## 4. 题库与标准答案

题目可来自公开量化 benchmark，或 AItrading 事故报告。当前尚未筛选具体来源；网页草案中的研究名称只作候选线索，后续选题时核实原文、答案质量和复用条件。

所有正式题目必须具备：

- 固定原始题面、必要上下文、输入数据及可复现代码起点。
- 明确的标准行为答案、得分关键点、分值与失败条件。
- 自动评分程序及对应的证据输出。
- 已知正确样例，以及能暴露关键错误的负向样例。
- 来源、提炼过程和答案确认记录。

标准答案指唯一明确的行为要求，不要求唯一代码写法。不同实现只要满足同样的行为和接口就应得到相同分数。文字理解题也尽量要求结构化事件判断或状态变化，避免仅凭另一个模型的审美打分。

事故报告是选题线索，不自动等于答案：需要确认当时预期、可复现触发条件、修复依据，再提炼成独立小包。存在未解决争议的事故不得进入正式计分题库。当前代码或历史补丁也不能未经核验就当标准答案。

同一发布版本给所有配置完全相同的题面、资料和评分点。harness 适配器只处理调用、路径和输出格式，不给特定模型补解释、降难度或换题。不兼容任务不能悄悄跳过后重新缩小分母。

题目中的业务要求对参赛者可见；具体标准输出、隐藏样例和评分实现由外部评分器持有。运行环境不暴露未来修复提交、其他运行答案或参考补丁。

## 5. 精简任务包结构

以下是首版候选结构，未作为已选定题目。暂定 6 包，每包约 3–5 个具有明确答案的关键点；实际题面以来源与答案核验为准。

| 包 | 主要目的 | 可能的关键点与输出 |
| --- | --- | --- |
| 1. 策略语义 | 自然语言理解 | 严格/包含边界、窗口范围、收线时点；输出事件判断 |
| 2. 状态路径 | 多事件逻辑理解 | 冷却起止、重复事件、状态转换；输出逐步预期状态 |
| 3. 策略实现 | 描述到代码 | 固定小型代码上下文，先提交行为判断再实现；分别评分 |
| 4. 事故修复 | 正确要求下的实现能力 | 由已确认事故提炼复现与预期；输出补丁和自测证据 |
| 5. 行为一致性 | 跨接口理解与实现 | 两条精简路径在指定事件下保持相同行为；输出修复 |
| 6. 审查判断 | 识别真问题和避免误报 | 固定的有错与无实质问题样例；输出问题位置、触发条件和判断 |

不要求每包都委派，不要求每包都执行三套不同轨道。理解、实现、Review 由题目结构和得分标签覆盖，一套包顺序跑完即可产生分项成绩。

每包固定可自测范围和交付要求，不设“30 分钟/60 分钟内完成”之类的性能时限，也不按目标分钟数倒推包规模。时间作为运行结果记录并进入综合评分。任务是否精简由题面、代码范围和关键点数量决定；真实复杂度来自明确的交易语义边界和必要依赖，不靠巨大仓库或无关上下文凑长度。

## 6. 运行控制：Codex 主持，固定程序执行

### 6.1 评估结论

统一的是运行合同、题面投递、结果格式及评分流程，不要求所有 harness 接受相同 CLI 参数，也不要求用户必须输入一条特殊命令。用户可以先配置好环境，再说“用配置 X 跑版本 Y 并出报告”。主持 Codex 将选择转换为运行清单，调用固定程序，最后解释结果。

不建议让主持 Agent 每包临场决定题面、催促方式、补充提示、是否重做或如何给分。这会把主持 Agent 的能力和偏好混入被测结果。正常执行应由小型、确定的运行器完成。

### 6.2 三个角色

| 角色 | 职责 | 与答案的关系 |
| --- | --- | --- |
| 主持 Codex | 登记用户选择、启动、查看运行状态、运行结束后解释报告 | 正式运行中只需配置与状态，不给参赛者做代码 Review 或提供修复建议 |
| 被测系统 | 指定 harness、模型、推理档，以及可选主控和子代理 | 只接触固定题面、允许资料和公开自测工具 |
| 运行器与评分器 | 固定顺序启动、收集状态和快照、离线验收、计分、入库 | 标准答案由独立评分器掌握，运行器按固定协议投递题面 |

如果用户指定“Codex 主控 + 某 Worker”，这个 Codex 是被测系统内部的新会话，负责 Review 和返工，其全部用量计入被测成绩。它与外层主持 Codex 职责不同。主持的管理与报告用量另列为 benchmark 开销，不混入某个候选的模型消耗。

### 6.3 固定流程与结束条件

流程为：预检 → 冻结运行清单 → 顺序启动各包 → 封存各包交付 → 独立评分 → 汇总入库 → 生成双榜和报告。

预检确认实际配置、工作目录、必要工具、身份与用量可观测性。每包使用新会话和干净起点。harness 适配器处理各自的启动、消息事件、子会话、输出与结束信号，不修改题意，也不改变用户指定的模型和认证路径。

正式任务约定明确的最终提交信号，适配器将其映射为提交事件并封存对应产物。Worker 完成只表示子任务交付，不表示整包结束。进程退出或停在等待状态也不自动等于合格交付。若 harness 需要后续 turn，按事先冻结的协议处理，不由主持自由追加“再仔细检查一下”。

没有完成时长硬限制。运行器可以报告运行中、明确失败、等待授权、已取消等状态；持续运行较慢不是失败。用户取消会留下未完成记录，不能伪装成完整成绩。基础设施错误与候选自身错误分开，不能因裁判坏了给候选零分。

在全部包结束前，不把隐藏评分反馈回被测系统。运行中正常自测、内部主控 Review 和纠正均允许，题目也可以提供统一的公开测试；外部隐藏裁判不充当免费修复助手。

若运行中需要主持判断业务、改代码、改题面或替候选解决问题，这次标记为人工干预运行；保留报告但不与无干预正式成绩混榜。主持可以修复评测设施问题，但该故障和后续处理须留存，不能透明擦掉失败后重跑取优。

### 6.4 当前可行性证据与未验证部分

本机 `codex exec --help` 已确认非交互运行、JSONL 事件、最终输出文件与结构化输出选项。OpenAI 官方非交互文档也说明 `--json` 事件流及保存的 CLI 认证可用于非交互执行。[S1] 因此 Codex 自动运行具有现成接入基础；完整子代理用量、快照捕获与具体隔离部署仍需实现时验证，不能据此称本 benchmark 已可运行。

其他 harness 按用户实际指定的环境验证其 CLI/会话接口；本轮没有验证全部候选兼容性。只有 UI 的环境需另评估稳定自动控制能力，不能默认与可重复的会话接口等价。先适配第一个指定环境，再逐个增加。

## 7. 评分和排名

### 7.1 最终正确性先决定资格，过程中返工再扣分

用户已明确：过程中做错但自我纠正，或者主控发现子模型错误后返工，应扣分但不等于最终失败；最终交付仍错误则不得分，严重性高于已修复错误。

用户已确认按包内独立题目判定：某题必要验收全部通过，该题才有分；最终未满足必要验收，该题能力分和综合分均为 0。同包其他正确题照常计分，不因一个题失败让整包或整套清零。题内逐关键点结果保留为诊断数据，但不以部分通过覆盖该题最终交付失败。

任务包是组织与执行单位，独立题目是零分和计分单位，关键点是题内验收证据单位。独立题目必须有可单独判断的交付和验收；共享代码的题目以封存最终产物分别验证各自要求，不在运行后通过拆题或合题改变分数。

每个包预先冻结必要要求、合法实现范围和答案，不以格式偏好或临时追加要求判最终错误。没有证据证明错误的情况不得乱扣；评分器错误使该评分无效，应修评分器而非罚候选。

拟议公式（结构提案，数值参数待定）：

- `A_i`：题 i 最终合格为 1，不合格为 0。
- `r_i`：按固定规则计算的可观测返工程度，范围 0–1。
- `lambda`：返工扣分上限比例，介于 0 与 1，发布前冻结。
- 题能力分 `Q_i = 100 × A_i × (1 - lambda × r_i)`。
- 包能力分为包内各题 Q_i 按冻结题目权重的加权平均；套件能力分再对各包等权平均。同时单列题目级最终正确率与首次正确率。第一版可默认包内各题等权，但必须在发布前固定。

这个结构使最终错误始终是 0；最终正确但经历返工仍有分；相同其他条件下，首次正确高于返工后正确。反复返工程度可封顶，额外时间和 token 仍会进入效率指标。不要仅用“最后都通过了”掩盖过程差异，也不要把正常推理中每个被放弃的想法当成一次答错。

理解、实现、审查标签提供分项视图。单代理与编排采用同样的交付标准和过程判定；单代理的自我修复同样记录，不能只对有 Worker 的配置扣返工分。

### 7.2 双榜同时存在（用户已确认）

每次有效运行同时产生两种成绩和排序：

- 能力榜：按最终验收与返工扣分后的能力总分降序，同分并列；单列最终正确率、首次正确率和返工率，并保留理解、实现、审查分项。
- 综合榜：正确性、token 资源效率、时间效率按固定权重形成综合分。时间是权重因素，不是硬性完成时限。

两榜都展示原始正确性、token、实际费用（若可得）、耗时及报告链接，支持各明细列单独排序。两个榜回答不同问题，不互相替代。

综合分以每题最终交付是否正确为前置条件，不能让错误题因低消耗获得效率分。拟议结构为：

`S_i = A_i × [wq × 100 × (1 - lambda × r_i) + wt × Etokens_i + wd × Etime_i]`

三个权重之和为 1。每包先按冻结题目权重汇总 S_i，套件再对各包等权平均。最终错误题三个部分均为 0；返工后正确的题可计分，但承担返工扣分和实际新增资源/时间。具体权重、返工扣分上限和效率换算参数须在正式发布前冻结。

若包内多题共用一次 Agent 会话，不能伪造精确的逐题 token/时间。此时 Etokens_i 与 Etime_i 使用该包的整体效率因子，按各题合格标记计入；整包全部真实资源照常保留，不因其中某题失败删去其消耗。若能按题隔离调用，则可采用题级计量，但同一版本必须固定方式，不能对不同模型临时切换。

效率换算采用随版本冻结的单调规则，不按当前榜单最优/最差值动态归一化。时间计分尺度不是运行时限。相同正确性和返工情况下，消耗更少、耗时更短分更高。

最终验收数据完整但返工不可观测时，仍可报告最终正确率；不能把未知返工当成零进入正式能力/综合排名。可单列“仅最终结果”成绩视图。token 等必需数据不全时，保留可计算的分项，不填零、不重新分配权重。完整榜只接纳相同过程观察协议与所需数据完整的运行。

排行榜行是“本次运行的完整配置”，展示 run_id、日期、模型/harness/推理档/模式、总分、分项分数、关键失败、用量完整性、token 与耗时、报告链接。单代理和编排可筛选，也可在相同评分规则下同表展示。

不要求有对照成绩才允许发布第一条成绩。以后其他配置完成同版本运行便可加入。单次得分是这次提交的表现，不声称是该模型的稳定能力估计；这不阻止排序，也不强制重复试验。

同一配置重复提交保留全部记录，不默默只取最高分；如提供每配置一行的简表，拟默认选最新有效运行并标明日期，历史明细始终可查。

只有相同题库、评分规则、代码起点及运行规则的成绩进入同一正式榜。不同版本分别展示；不得用换题后的分数直接宣称模型进步。机器资源和缓存状态记录在案，时间对比注明环境差异。

## 8. 返工率如何可比地测量

### 8.1 区分错误修正与正常开发

返工事件需有证据链：某个可评价的阶段产物违反了已知要求 → 自测、内部 Review 或后续行为识别了问题 → 后续产物修复了该问题。自我纠错和主控打回均属于这一机制，同时记录发现者与修复者。

不能用编辑次数、编译次数、测试失败行数、Worker 回合数直接代替返工次数。首次实现过程中尚未完成的代码、正常探索、新增功能或重命名不自动算错。单个错误导致多个测试失败，应按问题及关键点去重。同一错误被多个 Agent 提及也不能重复扣分；修复后再次引入则单列复发。

### 8.2 稳定的过程观察协议

正式发布前必须定义同样的可评分检查点，优先由自测、可评价候选产物、内部 Review 和最终提交等自然事件触发旁路快照。非完整草稿不作为初次候选。所有配置遵守同样的观察合同，运行器自动封存，不能让候选挑选只保存正确版本。除题目本身需要提交行为判断外，不强制模型先解释再编码或定期向主持交作业。

在检查点之间，适配器旁路保留与验证/Review 对应的产物快照。检查点及公开工具结果用于建立错误→修正证据；隐藏裁判在运行结束后离线判断，不把错误答案告知候选。

主指标建议分开记录：

- 返工发生率：至少出现一次有证据返工的已完成独立题目数 / 可完整观察的已完成独立题目数，同时列出分子、分母和缺失题。
- 题内返工程度 r_i：曾在已形成产物中错误、随后修复的评分关键点权重占比。重复修复/复发次数另列，是否额外扣分需固定规则，不按原始日志条数计分。
- 纠错结果：自纠后正确、主控纠正后正确、错误未修复、主控接管，以及观察不足。

首次候选已经错而最终仍错属于交付失败，不能因为看不到修复事件就表扬“返工率低”。最终正确率与返工率必须一起展示。

### 8.3 可观测性限制

无法可靠观测模型私有思考中推翻过多少错误。benchmark 测的是可观察产物和行动的返工，不声称测到所有心理过程。简单地问 Agent“你返工了几次”不能作为计分依据。

隐含自纠仍可能无法观测；旁路快照尽量不干预日常工作流。如果某 harness 必须增加显式提交阶段才能采集，应披露其影响并先评估是否适合，不能为了测量方便默认改变解题方式。首次实现前不应强制长篇思维链，理解题可提交简短结构化行为答案。

局部 Worker 只按其已冻结交接范围评价，不能因局部补丁未完成整包而误判。内部 Reviewer 指控错误不自动成立，需对照契约及产物；证据不足标 unknown。

不同 harness 若过程采集覆盖明显不同，不能以“日志更少所以返工更少”排名。先验证对应观察合同；不满足者保留结果但注明过程指标不可比。

### 8.4 资源与时间

完整记录被测系统内所有模型、自测、Review、内部返工及失败消耗，确认字段语义后去重。主持、评分和报告的开销另列。隐藏评分器执行时间不算候选作答时间；每包真实墙钟与整套用户等待时间分别保留。

原始 token、实际费用/额度及费率等价估算分别展示。返回缺失不填零。已有兼容成绩可以直接比较，不为比较自动追加其他配置运行。

## 9. 数据库与版本留存

建议首版使用本地 SQLite 保存结构化数据，题包、日志、快照和报告保存在有内容摘要的文件目录中，由数据库关联。SQLite 官方将单应用本地数据和应用文件格式列为适用场景；当前按用户指定配置顺序运行，不需要先引入远程数据库服务。[S2]

数据库最少表达以下实体，不要求把每个字段拆成独立复杂子系统：

| 实体 | 记录内容 |
| --- | --- |
| benchmark_release / pack_version / question_version | 发布版、包与独立题目关系、内容摘要、题面与必要答案版本 |
| scoring_version | 验收规则、返工观察协议、权重、归一化参数 |
| config_snapshot | 请求与实际模型、harness/插件版本、模式和资源环境 |
| run / pack_run / question_result | 一次启动、每包执行及题目级验收/分数、起止时间、配置和版本关联 |
| evidence / usage | 检查点、产物摘要、评分点结果、返工证据、用量完整性及文件引用 |
| evaluation | 指定评分版本对封存交付的评分、诊断结论和报告引用 |

发布版与运行记录不可覆盖。题目增加、答案修改或评分改变都记录新版本；不同版本成绩分榜。数据库只由可信运行器写入，被测环境不能读写它。

只改评分权重且旧证据足够时，可离线新增评分记录，不重新调用模型；必须标明“旧运行按新规则重算”，保留原分。新增题目或缺失新规则需要的过程证据时，不能靠重算补出成绩，需用户发起新运行。

排行榜是按版本与可比条件从记录查询得到的视图，不是唯一真相。所有提交均留存；同配置简表如用最新有效提交，注明规则与日期，不静默挑最高分。报告可由数据库和原始产物重新生成。

落盘顺序建议先保存不可变产物及内容摘要，再事务性登记记录，恢复时避免重复入库。备份必须包含数据库和它引用的产物；数据库结构迁移与 benchmark 题库版本是两种不同版本。

默认本地保存，不等于授权公开策略、题库或成绩。未来若确有多主机并发写入的需求再评估服务端数据库，不为假想规模提前建设。

## 10. 评测知情、答案泄漏与作弊边界

### 10.1 知道在测试不自动等于作弊

模型可能从题目形式和环境猜到这是 benchmark，无法保证其完全不知情。用户已确认：联网搜索和正常工程工具使用均允许，善用工具本身属于能力。查官方文档、搜索通用解法、安装正常依赖、编写和执行测试、调试及重构均可在已配置环境与权限内自由进行；benchmark 不规定解题路线。需要阻止的是获取评测专用标准答案、读取其他运行的解法、篡改裁判或成绩，以及主持在运行中泄漏额外提示。

### 10.2 主持和参赛者隔离

当前设计会话已知道评分设计，未来可能看过题目答案；不能把这个会话续作被测 Agent，也不能通过携带完整历史的 fork/subagent 开考。被测 Codex 使用独立新会话，仅投递该版本允许的信息；其他 harness 同理。

同一模型作为主持和参赛模型不自动构成泄漏；关键是会话、文件、记忆和工具权限有没有传递答案。主持在运行中仅看状态，运行结束后才读取评分明细解释报告；固定运行器负责发题，不让主持临场重新编写。

隔离至少包括：不挂载评分器、标准答案、数据库和历史报告；不读取个人记忆、旧会话及 AItrading 未来修复提交；仅提供题目所需工作副本与明确允许的工具。不能只建立另一个目录或一个 Git worktree 就声称读取已隔离。

涉及公开题或公开历史修复时，模型可能已从训练或检索见过答案。记录题目来源与暴露情况，不承诺零污染；新提炼任务和后续题库版本可降低风险，但同版各配置仍须同题同标准。

### 10.3 技术边界应可验证

实现时选择能保留指定 harness 与认证语义的独立 OS 用户、容器或虚拟环境，依据实际兼容性决定，不预先承诺所有桌面 harness 都可无损容器化。必须实测被测工具无法读取评测专用目录；写保护不等于读保护。

默认允许联网搜索和正常工程资源访问，不把网络限定为仅模型服务或狭窄的文档白名单。评测专用答案、历史运行产物和评分服务仍不对被测系统开放。通用公开资料属于合法工具输入；同题公开标准答案或原始修复补丁可能造成污染，应在选题时检查暴露情况，发现后标记受影响结果并换版，不能假装只靠一句“禁止作弊”就杜绝泄漏。

记录实际工具、Skills、MCP、网络与权限配置，作为完整参测配置的一部分。调用额外模型解决题目即产生模型协作，应登记并计量，不能借工具名义把它当成单模型成绩。普通搜索、编译器和测试程序本身不等于子代理；不限制主控内部如何选择工具或安排已声明的子代理。

候选提交封存后，在另外的工作副本进行验证。不能让候选代码与答案库、成绩数据库同权限运行；对需要执行的候选程序，用外部输入/输出行为比较，由可信评分器持有预期值和评分输出。被测代码不能自行写“passed”作为正式证据。

先用不含真实答案的探针确认读写隔离和产物边界，再称环境具备正式评测条件。未达到隔离要求的本地试跑仍可调试，但不能声称是防泄漏的正式成绩。

这降低作弊和无意泄漏的机会，不保证模型永远无法钻题目漏洞。若发现评分器遗漏真实要求，修订规则、标记受影响成绩，保留旧记录，不能临时只针对某个模型扣分。

## 11. 实施顺序与未决设计

本轮只完善文档和做只读可行性核实，没有运行被测模型、构建数据库、安装环境或改动生产路由。

下一步设计应按：落实题目级零分与包级汇总 → 定义可观察返工合同 → 选精简题包和确认答案 → 选第一个实际 harness 验证启动/采集/隔离 → 确定双榜权重并冻结版本 → 实现单次完整闭环。仍然不要求预设组合或多次运行。

已确认：最终错误按包内独立题目判零分，同包其他题独立计分；联网搜索和正常工程工具使用允许，自由选择解题路径。工具自由不授予评测答案和历史解法的访问权，也不意味着未登记的额外模型可以算作单模型。

剩余需求对齐集中于以下四项，不要求用户决定数据库表结构、运行器实现或计量适配细节：

1. 返工扣分边界：只扣违反题目行为要求的已修复错误，还是也扣语法、编译、依赖配置等工程错误？建议两类分别记录，行为错误进入主要返工扣分；普通工程试错主要反映在时间和 token 中，避免鼓励少测或不测。具体发生时点与可观测性由工程方案验证。
2. 评分取舍：能力/返工、token、时间的相对重要性，以及返工扣分的最大影响。建议先拿“全部正确但较慢”“少量最终错误但很快”“全对但多次返工”等虚构成绩请用户判断偏好，再提出固定权重和归一化规则；不要求用户凭空选百分比，也不使用真实参赛结果调权重。token 与实际货币成本仍分账，是否使用价格等价成本参与综合分须明确。
3. 首版内容代表性：理解判断、实现修复和审查判断各占多少；具体采用哪些公开题/事故案例。建议先只读筛选候选并列出来源、标准答案依据及公开暴露情况供确认，再冻结 5–8 个精简包；目前六包结构只是提案。
4. 首个实际接入配置：先支持哪个 harness、模型、推理档、模式，以及保留哪些日常 Skills/MCP/工具。建议在题库设计同时登记一个用户日常配置，验证自动启动和旁路采集；不会因此自动扩展组合。

第一项和第二项决定分数的含义，第三项决定榜单代表什么能力，第四项是实现接入顺序。用户可以继续提要求，不需要一次全部决定。

## 来源与证据边界

- [S1] [OpenAI 非交互运行文档](https://learn.chatgpt.com/docs/non-interactive-mode)：本轮已读取 JSONL 事件、结构化输出和认证说明；本机 `codex exec --help` 做了只读交叉检查。未据此声称全链路用量或隔离已验证。
- [S2] [SQLite 适用场景](https://www.sqlite.org/whentouse.html)：支持本地应用存储选型；具体数据实体是本草案建议。
- [S3] [Anthropic Agent 评测方法](https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents)：区分运行轨迹与最终环境结果，并指出共享历史会污染试验。本文仅借鉴这些方法，不引入其重复试验建议作为用户要求。


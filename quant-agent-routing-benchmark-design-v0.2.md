# 量化 Agent Benchmark 设计草案 v0.2

日期：2026-09-11。状态：需求对齐与设计，尚未实现、选定正式题目或运行测试。

本版依据用户本轮六项要求修订，是后续讨论的当前草案。[网页原始草案](quant-agent-routing-benchmark-plan-2026-09-11.md)保留为来源档案。与本版冲突的旧建议不再作为要求，尤其是预设 A/B/C 组合、自动排列模型矩阵、强制重复运行、扩充到 30–50 个任务家族，以及不提供总分排名。

## 1. 产品目标与确认的要求

建立一套可版本迭代、具有标准答案、统一计分的精简量化 benchmark。用户指定一个实际模型与 harness 配置，启动一次，系统按固定顺序执行该版本全部任务包，自动验收、计量、生成报告并登记成绩。以后指定其他配置运行同一版本，即可比较并排序。

| 用户要求 | 本版落实 |
| --- | --- |
| 系统化、版本化、量化复用，同题同标准 | 发布不可变的题库、答案、评分与运行规则版本；各配置使用相同内容 |
| 配好环境后统一启动，自动完成统计 | 一个运行入口，harness 适配器负责执行差异；自动顺序调度、评分和报告 |
| 支持单模型及不同 harness，按实际环境登记 | 成绩绑定实际模型、harness、推理档和模式，不要求覆盖所有搭配 |
| 用户指定什么跑什么，一次即可 | 一次启动仅运行一个配置，每个包一次；不自动发起对照组或重复试验 |
| 若干精简包、每包几个关键点 | 暂按 6 包设计，可在首版定稿时调整到约 5–8 包，不发展成长期大任务 |
| 最终有分数、排序，也保留详细报告 | 每次运行产生固定口径总分、分项得分、资源与时间明细，并进入对应版本成绩表 |

原有三个核心关切继续保留：策略理解是否正确；实现和返工发生了什么；整条执行链的质量、资源与效率如何。返工次数本身不等于错误次数，便宜也不等于正确。

## 2. 一次运行的含义

一次运行 = 一个 benchmark 版本 × 一个指定配置 × 固定任务包序列。

例如用户本次指定 OpenCode + DeepSeek v4.1 Flash + 某推理档 + 单代理，系统只跑这个配置。以后指定 Codex + 某主模型 + 某子代理，就另建一条运行记录。两次只有在题库和运行规则兼容时才直接比较。

“一次”不限制模型只能回答一条消息：包内可有正常工具调用、自测、自查，以及编排模式允许的内部返工；这些都计入同一包预算。禁止评分器公布答案后自动重做一遍取最好成绩。再次运行必须由用户另行发起，并保留原记录。

任务包之间使用独立会话和干净代码起点，按顺序执行，不把上一包答案、评语或探索历史带入下一包。一个包内部可以包含固定的阶段，例如先提交行为判断，再提交实现。

## 3. 被测配置与身份记录

最小配置包括：harness 名称与版本、模型与 provider、推理设置、执行模式，以及需要时的子代理配置。模型名以用户配置和实际回执为准，不由 benchmark 预置候选池。

支持两种执行模式：

- 单代理：一个被测 Agent 完成任务，可正常自测；不委派子代理。
- 编排：主控可按指定规则使用子代理，记录完整调用树及实际参与模型。允许某个包未发生委派。

同模型子代理也属于编排模式。若存在外部主控再委派的结构，同样记录为实际调用树，不额外触发新的测试配置。

每条成绩必须包含配置请求值、可观测实际值、harness/插件/路由规则版本和身份核验状态。推理档不受某 harness 支持时，应在启动前说明并停止该配置，不能静默换档。无法核实实际模型时保留 unknown，不能冒充已确认身份的模型成绩。

单模型能力通过“单代理模式下的该模型与该 harness”测量；同模型换 harness 形成另一条成绩。排行榜展示完整配置，不能把不同工具环境的差异全归因于模型能力。无需为登记一个模型补跑其他 harness。

## 4. 题库与标准答案

题目可来自公开量化 benchmark，或 AItrading 事故报告。当前尚未筛选具体来源；网页草案中的研究名称只作候选线索，后续选题时核实原文、答案质量和复用条件。

所有正式题目必须具备：

- 固定原始题面、必要上下文、输入数据及可复现代码起点。
- 明确的标准行为答案、得分关键点、分值与失败条件。
- 自动评分程序及对应的证据输出。
- 已知正确样例，以及能暴露关键错误的负向样例。
- 来源、提炼过程和答案确认记录。

标准答案指唯一明确的行为要求，不要求唯一代码写法。不同实现只要满足同样的行为和接口就应得到相同分数。文字理解题也尽量要求结构化事件判断或状态变化，避免仅凭另一个模型的审美打分。

事故报告是选题线索，不自动等于答案：需要确认当时预期、可复现触发条件、修复依据，再提炼成独立小包。存在未解决争议的事故不得进入正式计分题库。当前代码或历史补丁也不能未经核验就当标准答案。

同一发布版本给所有配置完全相同的题面、资料和评分点。harness 适配器只处理调用、路径和输出格式，不给特定模型补解释、降难度或换题。不兼容任务不能悄悄跳过后重新缩小分母。

题目中的业务要求对参赛者可见；具体标准输出、隐藏样例和评分实现由外部评分器持有。运行环境不暴露未来修复提交、其他运行答案或参考补丁。

## 5. 精简任务包结构

以下是首版候选结构，未作为已选定题目。暂定 6 包，每包约 3–5 个具有明确答案的关键点；实际题面以来源与答案核验为准。

| 包 | 主要目的 | 可能的关键点与输出 |
| --- | --- | --- |
| 1. 策略语义 | 自然语言理解 | 严格/包含边界、窗口范围、收线时点；输出事件判断 |
| 2. 状态路径 | 多事件逻辑理解 | 冷却起止、重复事件、状态转换；输出逐步预期状态 |
| 3. 策略实现 | 描述到代码 | 固定小型代码上下文，先提交行为判断再实现；分别评分 |
| 4. 事故修复 | 正确要求下的实现能力 | 由已确认事故提炼复现与预期；输出补丁和自测证据 |
| 5. 行为一致性 | 跨接口理解与实现 | 两条精简路径在指定事件下保持相同行为；输出修复 |
| 6. 审查判断 | 识别真问题和避免误报 | 固定的有错与无实质问题样例；输出问题位置、触发条件和判断 |

不要求每包都委派，不要求每包都执行三套不同轨道。理解、实现、Review 由题目结构和得分标签覆盖，一套包顺序跑完即可产生分项成绩。

每包固定可自测范围和交付要求，不设“30 分钟/60 分钟内完成”之类的性能时限，也不按目标分钟数倒推包规模。时间作为运行结果记录并进入综合评分。任务是否精简由题面、代码范围和关键点数量决定；真实复杂度来自明确的交易语义边界和必要依赖，不靠巨大仓库或无关上下文凑长度。

## 6. 统一自动化入口

用户体验：选一个配置和 benchmark 版本，启动后等待一份报告。

可提供统一命令，例如 `quant-bench run --suite quant-v1 --profile my-opencode-flash`。这是拟议接口，当前不存在该命令。自然语言入口可以把“用配置 X 跑 quant-v1”解析到同一个运行入口；不由主模型临场重写题目或调度规则。

自动运行流程：

1. 检查指定配置能否启动、模型身份能否观测、单代理/编排模式能否执行、环境依赖是否齐全。
2. 冻结题库版本、配置和本次 run_id。
3. 依次为各包建立干净环境，投递同一版本题面，启动指定 harness。
4. 采集回答、补丁、调用树、用量和时间；运行至明确交付或失败，不以预设完成时长截断正常运行。
5. 在参赛 Agent 无法读取的环境评分；保存逐点结果。
6. 汇总总分、分项分数、完整性与效率数据，输出报告并登记版本成绩表。

适配器负责启动、收集结果、响应用户取消以及回收本次测试状态。网络调用失联等基础设施故障按明确错误处理；不得把“运行较慢”自动认定为卡死。没有用户另行指定的限制时，不添加整套或单包的性能超时。无法支持自动执行的 harness 先标记为未支持，不假装通过同一入口已经兼容。按用户实际要测的 harness 逐个适配，不先搭四个 harness 八个模型的矩阵。

环境问题在启动前尽量拦截。启动后候选自身明确失败计入题目结果并继续后续包；用户主动中止则保留未完成记录，不作为完整提交进入正式榜；评分器自身故障标记为无效评分，不给候选记零分，也不生成正式总榜成绩。不得自动安装未知依赖、改写用户日常配置或切换认证路径。

正常评测应无需人工逐题裁决。第一版题目尽量消除运行时歧义；需要澄清的题只通过固定事实卡回答。若必须临时人工补充题意，该次保留诊断报告，并修订题目后再开展正式运行。

## 7. 评分和排名

### 7.1 固定正确性分数

拟议默认口径：每包 0–100 分，由该包预先发布的关键点权重计算；总分为各包得分的等权平均。增加隐藏样例不自动增加该关键点权重，避免“某包测试更多”就主导总分。

正式计分前冻结每个关键点的分值、部分得分条件和关键错误影响范围。关键错误可使相关行为项或该包失败，但不能由运行后 Reviewer 临时追加全局扣分。总分旁展示关键行为失败数，不能用高分掩盖明确的交易语义错误。

理解、实现、审查等标签产生分项分数，计算口径固定。同一行为在分项视图中出现不意味着再次叠加到总分。

单代理与编排采用相同任务、总分公式及运行规则，禁止为编排模式增加额外得分项。无子代理时，Worker 首次交付率和接管率记为不适用，不扣分。

### 7.2 双榜同时存在（用户已确认）

每次有效运行同时产生两种成绩和排序：

- 能力榜：按正确性总分降序，同分并列，展示理解、实现、审查分项及关键行为失败。
- 综合榜：正确性、token 资源效率、时间效率按固定权重形成综合分。时间是权重因素，不是硬性完成时限。

两榜都展示原始正确性、token、实际费用（若可得）、耗时及报告链接，支持各明细列单独排序。两个榜回答不同问题，不互相替代。

综合分的计算结构拟为 `S = wq × Q + wt × Etokens + wd × Etime`，三个权重之和为 1，Q 与两个效率分都使用固定的 0–100 标度。具体权重和效率换算参数尚待评分规则定稿，不在本轮擅自确定。

效率换算必须采用随版本冻结的单调规则，资源越少、耗时越短对应效率分越高；不得按当前榜单最优/最差值动态归一化，避免新增一个参赛配置就改写历史分数。时间归一化的参考量只是计分尺度，不是超时阈值。失败或空白提交不能靠用量低、耗时短取得高综合成绩，质量门槛或效率项的质量折算规则须在正式发布前一并冻结。所需用量缺失时综合分记为不可计算，不填零或重新分配其权重；能力榜仍可正常登记。

排行榜行是“本次运行的完整配置”，展示 run_id、日期、模型/harness/推理档/模式、总分、分项分数、关键失败、用量完整性、token 与耗时、报告链接。单代理和编排可筛选，也可在相同评分规则下同表展示。

不要求有对照成绩才允许发布第一条成绩。以后其他配置完成同版本运行便可加入。单次得分是这次提交的表现，不声称是该模型的稳定能力估计；这不阻止排序，也不强制重复试验。

同一配置重复提交保留全部记录，不默默只取最高分；如提供每配置一行的简表，拟默认选最新有效运行并标明日期，历史明细始终可查。

只有相同题库、评分规则、代码起点及运行规则的成绩进入同一正式榜。不同版本分别展示；不得用换题后的分数直接宣称模型进步。机器资源和缓存状态记录在案，时间对比注明环境差异。

## 8. 返工和效率的自动记录

记录各包第一次交付和最终交付。编排模式尽可能记录 Worker 首次补丁、主控 Review、打回、后续修正和接管边界；无法观察的事件标 unknown。

最终补丁按完整标准答案评分。局部 Worker 只承担部分目标时，不能因其补丁未满足整包要求就判 Worker 失败；仅对可对应其交接范围的评分点做首次交付判断，其他归因保持未知。

自动报告能确定的行为不合格、被改正的评分点、可观测返工次数和接管事件。主控误判、交接遗漏等原因需要具体证据，不强制让自动系统对每次返工给出确定归因。需要深入诊断时可另行研究，不阻塞总分和报告生成，也不默认追加运行。

资源计入主控、子代理及嵌套调用、Review、内部返工和失败调用；校准字段包含关系后去重，不能把缓存或 reasoning 重复相加。原始 usage 保留，缺失用量不填零。

分别展示 token 用量、可观测实际费用/订阅额度、按固定费率估算的参考成本。没有实际账单数据就不宣称真实支出。用量不完整仍可给正确性成绩，但不进入完整成本排名，也不报精确全链路节省比例。

每包从题面投递到最终提交或明确失败计墙钟；整次运行另记用户总等待时间。独立评分、环境准备时间分开列出。失败留在总耗时内，不只统计成功包。主动取消和外部故障保留已观测时长与未完成状态。

有历史兼容成绩时，可在报告中选择已有 run_id 比较分数差、token 差和时间差；该比较只用已有数据，不触发任何额外组合运行。正确性提高但 token 增加时明确呈现取舍，不一律称为节省。

## 9. 版本与留存

benchmark 发布版固定题面、任务包顺序、数据、代码起点、标准答案、评分器、得分权重、效率归一化参数和运行规则，保存内容摘要。任何可能改变得分或工作量的修改发布新版本，旧记录不覆盖。

适配器独立记录版本；适配器更新若改变题面、工具权限或预算语义，不能当作透明更新混榜。模型、harness、推理档和路由变化属于新的被测配置。

每次至少留存：运行清单、配置与实际身份、题面版本、回答与补丁、原始事件和用量、逐点评分证据、可观测返工记录、JSON 成绩、可阅读报告、失败原因。排行榜可从保存的成绩重新生成，不依赖聊天摘要作为唯一记录。

报告与排名默认本地保存。这里的“类似公开 benchmark”指评分与排名体验，不等于授权公开量化题目、策略、运行轨迹或成绩。

## 10. 实现范围与后续工作

实现采用一个运行器、固定任务包、按需添加的 harness 适配器、独立评分器以及报告/排名导出。优先完成统一合同，不在本阶段绑定 Harbor 或开发完整的新 Agent 框架。原草案的具体工具和仓库能力需在实际实现前验证。

后续准备顺序：细化双榜的计分权重与固定归一化规则；从公开来源或 AItrading 已确认事故筛选候选题；核定标准答案并冻结首版精简任务包；适配用户首先指定的一个环境；完成一次自动执行到报告与排名的闭环。新增模型配置无需重新出题，新增 harness 只补必要适配。

本轮仅维护设计文档，未授权启动付费评测、改动生产工作流或读取并向外部模型发送真实策略。

## 11. 最新确认与后续细化

用户已确认：能力榜与加权综合榜同时存在；不拿时间做硬约束，时间本身是综合评分权重之一。此前二选一的榜单问题及固定完成时长建议撤回。

后续需细化但不阻塞当前需求整理：综合分的具体权重、效率归一化规则及防止低质量低耗时获得高分的办法；首个模型/harness、精确题数和题目来源。这些是尚未定稿的设计参数，不应写成用户已经确认的要求。
